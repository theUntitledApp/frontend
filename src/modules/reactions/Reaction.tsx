import React, { FunctionComponent } from 'react';
import { Text, View } from 'react-native';
import { Reaction } from './reaction';

export const ReactionComponent: FunctionComponent<Reaction> = (props) => {
    return <View>
    </View>;
}